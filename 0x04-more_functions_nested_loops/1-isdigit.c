#include "main.h"
/**
 * _isdigit - check for digits
 * @x: Focused of the number
 * Return: 1 if true, else return 0
 */
int _isdigit(int x)
{
	if (x >= 48 && x <= 57)
	{
	return (1);
	}
	return (0);
}
